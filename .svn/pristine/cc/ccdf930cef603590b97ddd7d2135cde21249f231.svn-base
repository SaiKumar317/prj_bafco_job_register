// config.js
var ServerIP = `${window.location.hostname}`;
var httpPort = 7013;
var httpsPort = 7014;
var focusIP = `${window.location.host}`;

// Assign the variables to the global scope
window.ServerIP = ServerIP;
window.httpsPort = httpsPort;
window.httpPort = httpPort;

window.focusIP = focusIP;
